import { SimpleSearchPipe } from './simple-search.pipe';

describe('SimpleSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
